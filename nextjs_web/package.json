{
  "name": "web-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "db_start": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
